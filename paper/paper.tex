\documentclass[final]{nesy2026}
\usepackage[T1]{fontenc}
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{siunitx}
\usepackage{xcolor}
\usepackage{tikz}
\usepackage{forest}
\usetikzlibrary{calc, shapes.geometric, arrows.meta}

\newcommand{\uu}[1]{\underline{#1}}
\newcommand{\bb}[1]{\textbf{#1}}
\newcommand{\ub}[1]{\uu{\bb{#1}}}

\title[Evaluating Concept Grounding on Craftax]{Abstraction and Knowledge in Reinforcement Learning: Evaluating Concept Grounding on Craftax}

\clearauthor{\Name{Max Adamski} \Email{max.adamski@cs.put.poznan.pl}\\\addr Poznań University of Technology, Poland}

\begin{document}

\maketitle

\begin{abstract}
Symbolic reinforcement learning (RL) environments like Craftax provide interpretable observations that are ideal for reasoning.
In such gridworlds, tile vectors can be grounded into high-level concepts via an embedding bottleneck to abstract the state space and inject task-specific prior knowledge.
We investigate concept grounding with multi-label classification, using human-provided binary propositions as labels and a weighted binary cross-entropy loss to handle imbalance in the tile distribution.
We integrate concept grounding with Proximal Policy Optimization (PPO) via an additional loss term to allow both the classification loss and the PPO loss to influence embeddings.
We evaluate Craftax agents by comparing total episodic rewards (scores), achievement completion rates, and the total number of parameters. Embedding agents show average scores comparable to the baseline, but higher maximum scores, a 2x reduction in total parameters, and better generalization, completing more achievements in a rare level.
Grounded agents are more robust to initial conditions, showing that prior knowledge can stabilize reinforcement learning.
\end{abstract}

\section{Introduction}
\label{sec:intro}

PPO \citep{schulman2017} learns slowly on the challenging Craftax benchmark \citep{matthews2024}, where one source of difficulty is the wide variety of grid tiles that are discovered progressively as the agent gameplay improves.
As neuro-symbolic architectures promise improvements in RL \citep{acharya2024}, we investigate how knowledge affects learning by grouping related tiles using our prior knowledge of Craftax.
This should allow agents to transfer behaviors to new tiles, instead of having to learn purely by trial and error.
In general, we hypothesize that learning from abstracted inputs increases generalization (H1) and that prior knowledge guides learning (H2).
We test the hypotheses by evaluating our \emph{concept grounding} method, which forces agents to generalize across tiles using a compression bottleneck, and combines the PPO loss with an auxiliary multi-label classification loss to ground tile embeddings in an expert-defined taxonomy of high-level concepts.

More expressive Logic Tensor Networks \citep{serafini2016} have been used to inject prior knowledge and improve shape recognition through symbolic abstraction, although in simpler gridworlds \citep{badreddine2019,amador2025}. 
Concept Bottleneck Models (CBMs) also integrate human-defined concepts with neural networks \citep{koh2020}, having been used to avoid misalignment in Atari games \citep{delfosse2024}.
Overall, symbolic interfaces enable bidirectional human-AI interaction through advice and explanation mechanisms \citep{kambhampati2022}, and are effective in interactive settings \citep{guan2021}.
While these advanced methods are promising, we focus on evaluating the impact of knowledge on RL in a complex environment, using a simple method similar to CBMs.
Perceptual symbol grounding is used to transform visual observations into symbolic ones \citep{su2026}, but this is unnecessary in Craftax.

\section{Methods}
\label{sec:methods}

Consider the encoder-decoder architecture shown in \figureref{fig:architecture}.
It is designed for symbolic gridworld observations consisting of a sequence of tiles $e_1, e_2, \ldots, e_N$, where each tile is an interpretable $C$-dimensional vector ($x$), and each dimension is either binary or fuzzy ($0 \le x_i \le 1$).
The encoder is a neural network mapping tile vectors to $E$-dimensional tile embeddings ($z$), and the decoder network maps embeddings to $D$-dimensional logits, which become concept probabilities ($\hat y$) after applying the sigmoid function.
The ground truth ($y$) is computed from the original tile features ($x$), using expert-provided binary or fuzzy logic propositions.
The embeddings are optimized to predict concepts, and they introduce a compression bottleneck when $E < C$.
After independently preprocessing each tile, embeddings $e_1', e_2', \ldots, e_N'$ are used as the input of the downstream networks.

\begin{figure}[htbp]
    \centering
    \resizebox{0.8\textwidth}{!}{\input{include/grounding.tikz}}
    \caption{Concept grounding architecture for gridworlds}
    \label{fig:architecture}
\end{figure}

In \equationref{eq:concept_loss}, we define the loss that the encoder and decoder must minimize so that embeddings predict concepts.
It is a standard binary cross-entropy loss for imbalanced multi-label classification, averaged across labels. The concept loss is scaled by the hyperparameter $\lambda_{\mathcal K}$ and is added to the PPO loss ($\mathcal L = \mathcal L_{\text{PPO}} + \lambda_{\mathcal K} \mathcal{L_K}$).

\begin{equation}
  \mathcal{L_K} = \frac{1}{D} \sum_{i=1}^D \left[ -w_i y_i \log(\hat{y}_i) - (1 - y_i) \log(1 - \hat{y}_i) \right] \label{eq:concept_loss}
\end{equation}

In \equationref{eq:concept_weight}, we define the weighting scheme. Assuming imbalance in concept value distributions, each example is weighted inversely to the mean concept value in the minibatch ($\bar y_i$).
For binary concepts, the weight is proportional to the number of negative examples in the minibatch ($n_i^-$), and inversely proportional to the number of positive examples ($n_i^+$).
Negative examples are not weighted, so division by zero is avoided.

\begin{equation}
  w_i = \frac{1}{\bar y_i} - 1 = \frac{n_i^-}{n_i^+} \label{eq:concept_weight}
\end{equation}

In \figureref{fig:taxonomy}, we propose a taxonomy of Craftax tiles that contains our knowledge of the game.
Craftax observations mainly consist of 83-dimensional tiles $e_1, e_2, \ldots, e_{99}$, which are interpreted as an $11 \times 9$ grid.
For convenience, we assign lowercase names to dimensions $x_1, \ldots, x_{83}$, and capitalized names to concepts $y_1, \ldots, y_{19}$.
Among others, we define the \emph{Ranged} concept, grouping tiles occupied by similar enemies, with the binary proposition $\emph{Ranged} \leftrightarrow \emph{skeleton} \lor \emph{orc~mage} \lor \emph{gnome~archer}$. We also define concepts in terms of other concepts, like $\emph{Enemies} \leftrightarrow \emph{Ranged} \lor \emph{Melee}$, and using inequalities, like $\emph{Dark} \leftrightarrow \emph{light~level} < 0.05$. We implement these propositions as efficient tensor operations.

\begin{figure}[htbp]
  \floatconts{fig:taxonomy}
  {\caption{Proposed taxonomy for Craftax}}
  {\includegraphics[width=\linewidth]{include/taxonomy.pdf}}
\end{figure}


We compare the concept grounding architecture against a baseline non-recurrent PPO method.
To test H1, we separate the effect of abstraction introduced by the compression bottleneck, and to test H2 we remove guidance with prior knowledge by replacing supervised concept classification with self-supervised tile reconstruction.
In total, we evaluate the following four methods:

\begin{enumerate}
\item \textbf{Baseline}: the agent learns from raw observations.
\item \textbf{Compression}: the encoder compresses tiles, but the decoder is removed.
\item \textbf{Autoencoder}: compression, but a symmetric decoder reconstructs tiles ($y_i = x_i$).
\item \textbf{Grounding}: compression, but a single layer decoder grounds tiles in our taxonomy.
\end{enumerate}

For each method, we train 20 agents for 500 million steps, starting from different random seeds.
Notably, we use the same concept loss scale ($\lambda_{\mathcal K} = 0.5$) for both the autoencoder and grounding methods.
We then evaluate each agent for 100 thousand episodes across 4096 different worlds.
During evaluation, agents sample actions from their learned policy, which does not require the decoder.
After an episode ends, we record the binary vector of completed achievements and the score, defined as the normalized total episodic reward.\footnote{Positive rewards are given for completing achievements and negative rewards are given for taking damage. However, subtracted rewards can be reclaimed by healing. Thus, the maximum score of 100\% can be obtained by completing all achievements in the game with full health remaining.}
For each agent, we divide the number of evaluation episodes where a given achievement is completed, by the total number of evaluation episodes, to get the achievement rate.

\section{Results}
\label{sec:results}

\tableref{tab:methods} shows the tile dimension, the total agent parameter count (in millions), and the average and maximum score of each method (mean and standard deviation across agents).
Embeddings do not change the average score, but they do increase the maximum score.
We discuss parameter counts in \appendixref{apd:model_details} and report training times in \appendixref{apd:training_details}.

\begin{table}[htbp]
\floatconts{tab:methods}
{\caption{Evaluation summary}}
{\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{Tile Dim.} & \textbf{Params} & \textbf{Avg. Score} & \textbf{Max. Score} \\
\midrule
Baseline    & 83 & 9.54M & $11.62\% \pm 0.13\%$ & $22.75\% \pm 0.41\%$ \\
Compression & 32 & 4.39M & $11.82\% \pm 0.19\%$ & $25.24\% \pm 1.00\%$ \\
Autoencoder & 32 & 4.42M & $11.77\% \pm 0.17\%$ & $25.27\% \pm 1.20\%$ \\
Grounding   & 32 & 4.39M & $11.87\% \pm 0.18\%$ & $26.42\% \pm 1.15\%$ \\
\bottomrule
\end{tabular}}
\end{table}

In \figureref{fig:analog}, we show the completion rates for analogous achievements in common and rare scenarios (mean across agents, individual values, and 95\% confidence intervals).
Embeddings slightly increase achievement rates after entering a common level, and greatly increase analogous achievement rates after entering a rare level.\footnote{To compare rates across levels, we show rescaled achievement rates $P(A|B) = P(A)/P(B)$, where completing achievement $A$ requires first completing achievement $B$. For reference, an average grounded agent enters the gnomish mines in only 230 episodes during the 100 thousand episode evaluation.}
All grounded agents learn to complete analogous achievements at similar rates in both levels, while a few compression and many autoencoder agents show degraded performance in the rare level.

\begin{figure}[htbp]
  \floatconts{fig:analog}
  {\caption{Completion rates for analogous achievements}}
  {\includegraphics[width=\linewidth]{include/abtest_analog.png}}
\end{figure}


In \figureref{fig:iron_achievements}, we show that compression degrades performance for most agents on two common achievements and one rare achievement, but grounding can limit the effect.

\begin{figure}[htbp]
  \floatconts{fig:iron_achievements}
  {\caption{Completion rates for iron-related achievements}}
  {\includegraphics[width=\linewidth]{include/abtest_iron.png}}
\end{figure}

\section{Conclusions}
\label{sec:conclusions}

The average score remains at the level of the baseline for all of the investigated embedding methods.
However, compressing tiles in Craftax greatly decreased parameter counts and allowed agents to group tiles to complete analogous achievements regardless of level and tile rarity.
Additionally, concept grounding stabilized learning with prior knowledge of the environment.
We report the complete evaluation results in \appendixref{apd:eval}.

The difference between compression and concept grounding shows that jointly learning multi-label classification for an expert-designed taxonomy can have a stabilizing effect and can even partially recover performance lost in bottlenecks.
Conversely, the autoencoder shows that learning to reconstruct tiles does not stabilize learning in a symbolic environment where tiles do not contain visual patterns.
Further results in \appendixref{apd:extra} show that misleading guidance mainly hurts rare-level performance without preventing learning.

We see that learning from abstracted inputs can increase generalization (H1), as supported by significant improvements in completion rates for a class of Craftax achievements and dramatic improvements in a rare level where new tiles are introduced.
We also find that prior knowledge can guide learning (H2), as shown in a rare level where unguided agents can fail to complete analogous achievements simply because of a random seed, as well as in a common level where most unguided embedding agents fail to complete iron-related achievements, while many grounded agents succeed.

RL with PPO can be unstable, as shown by highly variable scores across training runs on some Atari games \citep{schulman2017}, and as we see with embedding agents on some Craftax achievements.
In this context, we consider concept grounding to be a semantic regularizer, as it reduces sensitivity to random seeds. However, its applicability is limited to symbolic observations and it requires additional modeling and knowledge engineering.

Future work may investigate more expressive architectures and knowledge bases with recurrent Craftax models.
Although these models achieve higher scores, they remain insufficient to fully solve this benchmark, even on a large 10 billion step training budget.
Beyond Craftax, concept grounding could be applied in other symbolic environments with a large variety of potentially rare objects.
More research is needed to see whether concept grounding could be combined with symbol grounding to stabilize learning in visual environments, whether the knowledge engineering process could be automated, or whether some knowledge could be transferred between environments.
We publish our code and data online.\footnote{\url{https://github.com/maxadamski/concept-grounding}}

\acks{We thank Jędrzej Potoniec and Wojciech Kotłowski for advice and feedback, and the Poznań University of Technology for GPU access and financial support (SBAD/0770).}

\bibliography{paper}

\appendix

\section{Architectural details}\label{apd:model_details}

The downstream actor and critic networks have 3 layers, each with 512 neurons and ReLU activation.
Craftax has an 8268-dimensional observation space ($9 \times 11 \times 83$ for tiles and 51 for global state), so the actor and critic contain $8268 \times 512 \times 2$ weights in their first layers, which account for the majority of agent parameters.
The encoder accepts 83-dimensional inputs, consists of 3 layers, each with 83 neurons and ReLU activation, and outputs 32-dimensional embeddings.\footnote{Flattened embeddings are concatenated with the 51-dimensional global state for downstream processing.}
Embedding results in a 3219-dimensional observation space, which proportionately reduces the number of weights in the first layers of the actor and critic.

\section{Training details}\label{apd:training_details}

In \figureref{fig:train_plots}, we show achievement rates during training (mean and standard deviation across agents). To reduce noise, we apply a running average with a window size of 40 batches.

We train each agent for 500 million steps across 1024 different environments.
We perform rollouts for 64 steps, resulting in batches of 65536 steps.\footnote{We prevent illegal actions during rollouts by setting their logits to $-\infty$ before applying softmax.}
Each batch is divided into 16 minibatches of 4096 steps.
Each step contains an observation with 99 tiles, meaning that the encoder and the decoder are trained on large minibatches of tiles.

Introducing the encoder and decoder increased training times for our PyTorch implementation. The training time for baseline agents is $4.6 \pm 0.6$ hours (mean and standard deviation), while the training time for compression agents is $6.7 \pm 0.2$ hours, which is roughly a 50\% increase on average. The training time for autoencoder agents is $7.9 \pm 0.9$ hours, which is roughly a 70\% increase relative to the baseline, and roughly a 20\% increase relative to compression, which we attribute to the symmetric decoder network. Grounding training time is roughly comparable to compression, as agents finish training in $6.2 \pm 0.4$ hours.

Each agent is optimized end-to-end with AdamW, with a constant learning rate during training ($\eta = 2\cdot10^{-4}$), and with default values for the remaining optimizer hyperparameters.\footnote{\url{https://docs.pytorch.org/docs/2.9/generated/torch.optim.AdamW.html}}
Following \cite{matthews2024}, we reuse each batch for 3 epochs, and we use the same values for generalized advantage estimation ($\lambda = 0.8$), the discount factor ($\gamma = 0.99$), maximum gradient norm (1.0), and PPO hyperparameters for policy update clipping ($\epsilon = 0.2$), scaling the value loss ($c_1 = 0.5$), and scaling the entropy bonus ($c_2 = 0.01$).

\section{Evaluation details}\label{apd:eval}

We consider a set of 42 achievements.
Because no agents advanced beyond the \emph{gnomish mines} level, we exclude achievements that can only be completed in later levels.
For completeness, we present the evaluation results for all considered achievements in \figureref{fig:eval_plots}.

For each method, we train a population of agents ($n = 20$) using different random seeds.
We then apply the non-parametric, two-sided Mann-Whitney U test to support our claims of statistically significant changes in achievement completion rates relative to the baseline method.
A significant result for a given achievement indicates that the two methods have distinct completion rate distributions, accounting for the effects of random initialization during training and randomness of the environment.

We perform three families of tests, independently comparing the baseline method's achievement rates with those of the compression, autoencoder, and grounding methods.
For each family, we apply the Bonferroni-Holm correction to reduce the probability of false-positive findings when performing multiple comparisons.

In \tableref{tab:tests}, we report the mean and standard deviation of the achievement rates across the baseline agents (in \%).
For the embedding methods, we report the change in the mean achievement rate in percentage points ($\Delta$p.p.).
For each achievement, we underline statistically significant changes ($p < 0.05$) and highlight the change that results in the highest mean completion rate, indicating the best embedding method for that metric.
For certain achievements, a negative change is highlighted, which indicates that the baseline outperforms the embedding methods.
To reduce noise, we omit highlighting for achievements with no statistically significant changes.

Notably, iron collection rates do not significantly differ between methods during evaluation, indicating that iron is recognized and collected, but crafting probabilities drop significantly for agents using compression.
Additionally, in \figureref{fig:train_plots} we see that compression and autoencoder agents learn to collect iron faster than grounded agents.
This suggests that the learned iron tile embedding is not the main cause of unstable tool crafting.
However, due to confounding factors in this complex environment, we cannot conclusively determine why compression degrades iron tool crafting achievement rates.

\section{Further results}\label{apd:extra}

We designed the Craftax taxonomy with completeness in mind, ignoring tiles that can only be found in levels beyond the \emph{gnomish mines}.
However, we later found completeness unnecessary, as we could prune the \emph{Static}, \emph{Liquid}, \emph{Backward}, \emph{Unlocked}, \emph{Important}, and \emph{Dark} nodes, and the \emph{chest}, \emph{wood}, and \emph{plant} leaves, without affecting achievement rates.

Correctness of the prior knowledge was another concern, as misleading guidance could make learning more difficult than necessary.
Using the same training and evaluation steps, we trained 20 additional agents using a randomized taxonomy with the same structure as shown in \figureref{fig:taxonomy}, but with leaves randomly shuffled to produce nonsensical groups.

In \tableref{tab:shuffled} and \figureref{fig:extra}, we compare the original concept grounding method with the shuffled variant using the same statistical testing methodology as in \tableref{tab:tests}.
As there are no other significant changes, we only show achievements discussed in the main text.\footnote{We also exclude P(make iron armour) from the table due to near-zero values.} 

Surprisingly, the shuffled taxonomy did not completely prevent learning, as the resulting agents achieve an average score of $11.65\% \pm 0.16\%$ (mean and standard deviation across agents) and a maximum score of $25.31\% \pm 1.90\%$, which is roughly comparable to the autoencoder variant.
As expected, we see that grounding tiles in a nonsensical taxonomy has a negative effect in the rare level and a significant but small effect in the common level, as proper grouping is the key to completing analogous \emph{eat} and \emph{defeat} achievements.
However, there are no significant changes in iron-related achievement rates when compared to the original taxonomy.
One possible explanation is that feature selection for auxiliary supervision could be primarily responsible for recovering performance in the bottleneck.

\begin{figure}[htbp]
  \floatconts{fig:train_plots}
  {\caption{Completion rates for all considered achievements during training}}
  {\includegraphics[width=\linewidth]{include/training.png}}
\end{figure}

\begin{figure}[htbp]
  \floatconts{fig:eval_plots}
  {\caption{Evaluation results for all considered achievements}}
  {\includegraphics[width=\linewidth]{include/abtest.png}}
\end{figure}

\begin{table}[htbp]
\centering
\small
\setlength{\tabcolsep}{4pt}
\caption{Statistical tests for changes in achievement rates relative to the baseline}
\label{tab:tests}
\vspace{10pt}
\begin{tabular}{lcrcrcrc}
\toprule
& \textbf{Baseline} & \multicolumn{2}{c}{\textbf{Compression}} & \multicolumn{2}{c}{\textbf{Autoencoder}} & \multicolumn{2}{c}{\textbf{Grounding}} \\
\textbf{Metric} & $\mu\pm\sigma$ (\%) & $\Delta$p.p. & $p$-val & $\Delta$p.p. & $p$-val & $\Delta$p.p. & $p$-val \\
\midrule
P(collect wood)          & 99.53 $\pm$ 0.11 &      0.06  &     0.9781 &      0.09  &     0.3332 &      0.05  &     1.0000 \\
P(make wood sword)       & 89.19 $\pm$ 1.05 &      1.00  &     0.1311 &      0.58  &     1.0000 &      0.58  &     1.0000 \\
P(make wood pickaxe)     & 95.80 $\pm$ 0.46 &     -0.01  &     1.0000 &      0.28  &     0.9444 &      0.20  &     1.0000 \\
P(place stone)           & 90.51 $\pm$ 0.80 &     -0.39  &     1.0000 &     -0.25  &     1.0000 &      0.05  &     1.0000 \\
P(place table)           & 97.65 $\pm$ 0.40 &      0.26  &     0.5043 &      0.33  &     0.1420 &      0.14  &     1.0000 \\
P(place furnace)         & 89.46 $\pm$ 1.21 &     -0.30  &     1.0000 &      0.40  &     1.0000 &     -0.01  &     1.0000 \\
P(collect stone)         & 94.61 $\pm$ 0.54 &     -0.27  &     1.0000 &      0.10  &     1.0000 &      0.10  &     1.0000 \\
P(make stone sword)      & 86.87 $\pm$ 1.32 &     -0.34  &     1.0000 &     -0.24  &     1.0000 &      0.21  &     1.0000 \\
P(make stone pickaxe)    & 68.28 $\pm$ 2.62 &      0.15  &     1.0000 &     -0.66  &     1.0000 &     -1.31  &     1.0000 \\
P(collect coal)          & 74.61 $\pm$ 0.79 &  \bb{2.59} & \uu{0.0001}&      2.29  & \uu{0.0001}&      2.38  & \uu{0.0001}\\
P(make torch)            & 84.78 $\pm$ 0.77 &  \bb{1.98} & \uu{0.0001}&      1.87  & \uu{0.0001}&      1.09  & \uu{0.0091}\\
P(place torch)           & 82.39 $\pm$ 0.98 &  \bb{2.50} & \uu{0.0001}&      2.21  & \uu{0.0001}&      1.40  & \uu{0.0112}\\
P(collect iron)          & 53.64 $\pm$ 1.31 &     -0.89  &     1.0000 &     -0.40  &     1.0000 &      0.94  &     1.0000 \\
P(make iron sword)       & 20.16 $\pm$ 2.31 &     -13.42 & \uu{0.0016}&     -15.41 & \uu{0.0001}& \bb{-5.35} &     1.0000 \\
P(make iron pickaxe)     & 10.93 $\pm$ 1.57 &     -6.44  & \uu{0.0002}&     -6.98  & \uu{0.0002}& \bb{-3.58} & \uu{0.0086}\\
P(make iron armour)      &  0.04 $\pm$ 0.02 &     -0.04  & \uu{0.0001}&     -0.04  & \uu{0.0001}& \bb{-0.03} & \uu{0.0001}\\
P(find bow)              & 58.26 $\pm$ 1.38 & \bb{-0.70} &     1.0000 &     -1.13  &     0.3332 &     -2.61  & \uu{0.0008}\\
P(make arrow)            & 67.17 $\pm$ 2.59 &  \bb{7.21} & \uu{0.0001}&      6.45  & \uu{0.0001}&      6.36  & \uu{0.0001}\\
P(fire bow)              & 32.12 $\pm$ 1.50 &  \bb{4.24} & \uu{0.0001}&      3.28  & \uu{0.0010}&      1.89  & \uu{0.0364}\\
P(collect drink)         & 76.88 $\pm$ 1.80 &      0.00  &     1.0000 &      1.37  &     0.1829 &      0.78  &     1.0000 \\
P(collect sapling)       & 86.26 $\pm$ 2.56 &      3.74  & \uu{0.0003}&      3.94  & \uu{0.0005}&  \bb{4.22} & \uu{0.0001}\\
P(place plant)           & 84.85 $\pm$ 2.62 &      4.95  & \uu{0.0001}&      5.15  & \uu{0.0001}&  \bb{5.26} & \uu{0.0001}\\
P(eat plant)             & 02.81 $\pm$ 0.44 &  \bb{1.81} & \uu{0.0001}&      1.71  & \uu{0.0001}&      1.65  & \uu{0.0001}\\
P(wake up)               & 38.81 $\pm$ 8.64 &     -1.61  &     1.0000 &     -1.35  &     1.0000 &      4.04  &     0.7912 \\
P(eat cow)               & 64.08 $\pm$ 1.03 &      4.33  & \uu{0.0001}&      4.98  & \uu{0.0001}&  \bb{5.05} & \uu{0.0001}\\
P(defeat zombie)         & 45.29 $\pm$ 0.90 &      2.78  & \uu{0.0001}&      2.99  & \uu{0.0001}&  \bb{3.41} & \uu{0.0001}\\
P(defeat skeleton)       & 42.70 $\pm$ 0.77 &      3.47  & \uu{0.0001}&  \bb{3.72} & \uu{0.0001}&      3.50  & \uu{0.0001}\\
P(enter dungeon)         & 60.60 $\pm$ 1.42 & \bb{-0.79} &     1.0000 &     -1.17  &     0.2903 &     -2.74  & \uu{0.0004}\\
P(open chest|dungeon)    & 96.14 $\pm$ 0.28 &      0.09  &     1.0000 &     -0.01  &     1.0000 &      0.04  &     1.0000 \\
P(eat snail|dungeon)     & 80.77 $\pm$ 1.45 &      3.78  & \uu{0.0001}&  \bb{4.24} & \uu{0.0001}&      4.04  & \uu{0.0001}\\
P(defeat orc soldier|...)& 72.20 $\pm$ 2.39 &  \bb{5.40} & \uu{0.0001}&      5.07  & \uu{0.0001}&      5.23  & \uu{0.0001}\\
P(defeat orc mage|...)   & 52.24 $\pm$ 3.23 &      6.19  & \uu{0.0001}&      6.36  & \uu{0.0001}&  \bb{6.82} & \uu{0.0001}\\
P(enter g. mines)        &  0.03 $\pm$ 0.02 &      0.11  & \uu{0.0001}&      0.16  & \uu{0.0001}&  \bb{0.20} & \uu{0.0001}\\
P(eat bat|mines)         &  3.14 $\pm$ 4.43 &     55.35  & \uu{0.0001}&     46.19  & \uu{0.0001}& \bb{60.26} & \uu{0.0001}\\
P(defeat g. warrior|...) &  2.96 $\pm$ 3.66 &     52.78  & \uu{0.0001}&     54.11  & \uu{0.0001}& \bb{62.90} & \uu{0.0001}\\
P(defeat g. archer|...)  &  0.48 $\pm$ 1.22 &     26.93  & \uu{0.0001}&     22.00  & \uu{0.0001}& \bb{37.44} & \uu{0.0001}\\
P(collect ruby)          &  7.30 $\pm$ 0.38 &     -0.23  &     0.9584 &     -0.30  &     0.1852 &     -0.37  &     0.0635 \\
P(collect sapphire)      &  7.33 $\pm$ 0.35 & \bb{-0.26} &     0.3000 &     -0.33  &     0.0512 &     -0.41  & \uu{0.0175}\\
P(collect diamond)       &  8.72 $\pm$ 0.39 & \bb{-0.27} &     0.5005 &     -0.35  &     0.0846 &     -0.47  & \uu{0.0183}\\
P(make dia. sword)       &  1.02 $\pm$ 0.05 & \bb{-0.04} &     0.2832 &     -0.05  &     0.0581 &     -0.07  & \uu{0.0112}\\
P(make dia. pickaxe)     &  1.03 $\pm$ 0.04 & \bb{-0.05} &     0.0867 &     -0.06  & \uu{0.0213}&     -0.08  & \uu{0.0021}\\
P(make dia. armour)      &  3e-4 $\pm$ 5e-4 &   -2.0e-4  &     1.0000 &    -1.5e-4 &     1.0000 &   -2.5e-4  &     0.6294 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[htbp]
\small
\floatconts{tab:shuffled}
{\caption{Selected rates for grounded agents using the original and shuffled taxonomies}}
{\begin{tabular}{l r@{\,$\pm$\,}l r@{\,$\pm$\,}l r c}
\toprule
\textbf{Metric} & \multicolumn{2}{c}{\textbf{Grounding}} & \multicolumn{2}{c}{\textbf{Shuffled}} & \textbf{$\Delta$p.p.} & \textbf{$p$-val} \\
\midrule
P(collect iron)           & 54.58\% & 2.83\% & 54.21\% &  2.96\% &  -0.37 &     1.0000 \\
P(make iron sword)        & 14.81\% & 9.24\% &  9.80\% &  9.31\% &  -5.01 &     1.0000 \\
P(make iron pickaxe)      &  7.35\% & 3.47\% &  5.87\% &  4.16\% &  -1.47 &     1.0000 \\
P(enter dungeon)          & 57.85\% & 1.64\% & 57.31\% &  2.13\% &  -0.54 &     1.0000 \\
P(eat snail|dungeon)      & 84.81\% & 0.87\% & 83.44\% &  1.22\% &  -1.37 & \uu{0.0208}\\
P(defeat orc soldier|...) & 77.43\% & 0.93\% & 76.31\% &  2.54\% &  -1.12 &     1.0000 \\
P(defeat orc mage|...)    & 59.06\% & 1.30\% & 56.77\% &  2.56\% &  -2.29 & \uu{0.0247}\\
P(enter gnomish mines)    &  0.23\% & 0.07\% &  0.17\% &  0.05\% &  -0.06 &     0.3557 \\
P(eat bat|enter g. mines) & 63.40\% & 3.90\% & 39.63\% & 18.70\% & -23.76 & \uu{0.0001}\\
P(defeat g. warrior|mines)& 65.86\% & 3.82\% & 36.26\% & 27.41\% & -29.60 & \uu{0.0013}\\
P(defeat g. archer|mines) & 37.91\% & 3.13\% & 17.21\% & 14.65\% & -20.70 & \uu{0.0004}\\
\bottomrule
\end{tabular}}
\end{table}

\begin{figure}[htbp]
  \floatconts{fig:extra}
  {\caption{Selected achievement rates for all method variants}}
  {\includegraphics[width=\linewidth]{include/abtest_extra.png}}
\end{figure}

\end{document}
